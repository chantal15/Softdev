<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asiapacific.attendancemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".LoginProfessor" />
        <activity
            android:name=".ProfMainActivity"
            android:label="@string/title_activity_prof_main" />
        <activity android:name=".ClassesAddClass" />
        <activity android:name=".ProfStudentsList"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".StudentMainActivity" />
        <activity android:name=".LoginStudent"></activity>
    </application>

</manifest>